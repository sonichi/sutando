{
  "name": "ag2space-voice",
  "version": "1.0.0",
  "owner": "github:sonichi/sutando",
  "license": "MIT",
  "stability": "experimental",
  "access_tier": "owner",
  "enabled": true,
  "description": "AG2 Space voice surface: binds the room the desktop client announces (session.context) to the session's origin on the gateway bridge's membership verdict, so delegated work is answered in that room. Optional: the host runs unchanged without it.",
  "permissions": {
    "network": false,
    "filesystem": "read-write",
    "secrets": "none"
  },
  "tools": "./tools.ts",
  "provenance": {
    "source_repo": "https://github.com/sonichi/sutando"
  }
}

# CHANGELOG-PENDING

Unreleased changes to be curated into the next `CHANGELOG.md` entry.

**Instructions for contributors:** add a one-line entry under the appropriate section when your PR introduces a user-visible change. PR number in brackets at the end. Entries are curated (not auto-generated) before each release.

Format: `- Brief description of what changed. ([#NNN])`

---

## Added

<!-- feat() PRs go here -->
- marketplace: Sutando can install marketplace skills, activate cloud tools, check for outdated skills, update, and uninstall on request or from a use-case doc's `Requires:` list, then tells the owner once if a core restart is needed.
- import-claude-context: new in-box, user-invocable skill that brings the owner's Claude Code history into Sutando — an LLM-free index of the stock `~/.claude/projects` transcripts, noise-stripped + secret-redacted 0600 dialog dumps, haiku summaries per session/project/entities, and sinks in core memory (≤2 KB file + budget-guarded `MEMORY.md` row), `notes/claude-import/` and People payloads; read-only on `~/.claude`, `--new` incremental, `--forget <slug>` undo. Review gate: `finalize.py` stages by default under `data/claude-import/staged/` (with a `review.md` digest and duplicate people/companies merged first) and writes the sinks only on the owner's "bring it in" (`--commit`, `--projects` for one project, `--discard` to drop). `session-recap/scripts/extract.py` gains `--root`; `context_resume` exports `message_text`/`clean_text`/`NOISE_*_RE`.
  - Counting: `extract.py` counts a session as `extracted` only when it wrote a dump — a session with no cleaned dialog or under `--min-chars` (400) is `skipped_empty`, remembered in `state.json` so `--new` leaves it alone (the fresh-install run said 47 extracted with 43 dumps on disk); `index.py` reports `empty` (no assistant message) and `conversations` = sessions − empty, the number the acknowledgement uses (`--counts-only` stays a file count); `finalize.py` leaves skipped sessions out of every session count and they never block `--commit`; new `progress.py` recounts summaries / roll-ups / entities / staging from disk and rewrites `status.json` (phase `summarizing` → `rolling-up` → `staged`) after each coordinator batch, so the desktop's progress no longer sits at `summarized 0`.
  - People store: an existing person is never overwritten or duplicated — `finalize.py --stage --known-people <file>` takes the station's `people__list_people` listing and matches every staged person by email, then by normalised name (diacritics folded); matches become UPDATE payloads (same slug/id, merged `identifiers.emails`, a `doc_append` section and no `doc`) that the coordinator merges into the fetched dossier with `--people-doc-merge` (same-heading section replaced, idempotent); a name two store entries share is "Needs your call" and is never upserted; the review lists "Already in your People store" and "New" separately (the 25 cap applies to new people only) or says "Store not checked" when the People tool was unavailable.
  - Review-gate hardening (PR #4127 review): the memory file and `overview.md` are rebuilt from per-project **approved snapshots** (`data/claude-import/approved/<slug>.json`, written by `--commit`), so committing one project never lands another's unreviewed roll-up re-run — the digest names it "changed since approval — bring in <slug> to refresh"; `--forget` takes a known slug (or a unique part of one) and refuses paths, `..`, `~`, unknown or ambiguous values, symlinks and any target resolving outside `notes/claude-import/`, `data/claude-import/` or the memory dir before deleting anything; `index.py` redacts every metadata string (titles, prompts, summary, agent name) with the importer's policy (`_common.redact_text`, the same object `extract.py` uses) before cutting it to length; `--forget`, `--forget-session` and `--hold` shrink the approved People export (`people.json`) to the citations still approved (`people_revoked` counts the removals) — starting from the published set, never from a re-ranking of `approved/people-inputs.json`, so a removal never surfaces a person the cap had hidden from the digest — and `--forget-session` drops the session's entity citations too; a commit grows that export only with the projects it lands — every other landed project keeps the citations in `approved/people-inputs.json` (whose per-project `people_hashes` let the digest name a landed project whose people changed since approval, under People: "bring in <slug> to refresh"), so an entities re-run between two commits never lands an unreviewed person or citation, and `--people-json --projects <landed slug>` prints the approved payloads.
    - What `--commit` publishes to `people.json` is now exactly what the digest showed (PR #4127 follow-up): `--stage` computes the **published** people set the way the commit will — the selected projects' live citations/fields unioned with every landed project's approved citations/fields — so a person who clears the two-citation floor only through that union, or whose role/company/emails change, appears in `staged/people.json` and under a new "Updates to people already in your export" section of `review.md` with the concrete field diff; a full `--commit` then publishes the staged set verbatim (fingerprint-guarded, no recompute from live `entities.json`), and a person the digest did not list keeps their approved fields byte-for-byte. Previously the stage showed a selected-only set below the floor while the commit unioned in and published an unreviewed field change.
    - Two adjacent cases closed (PR #4127 review): a `--commit --projects <slug>` subset no longer recomputes the capped top-25 over the narrowed projects (which could publish a person the combined digest's cap had hidden) — it publishes only people from `staged/people.json`, cut to the landed projects' citations, and `--people-json --projects <landed slug>` prints only people the export published; a project-only view needs `--stage --projects <slug>` and that digest's yes (SKILL.md step 10). The digest's field-diff rows compare the values the dossier publishes (role/company at 80 chars, relationship at 200) instead of a 60-char preview, and show a long value as its shared prefix elided plus both tails, so a role whose suffix changed from "APPROVED" to "UNREVIEWED" is shown before it lands.
  - Personal sessions are held: the session summary carries `personal` / `personal_reason` (a generic category, ≤ 6 words); a held session contributes nothing to memory, notes, entities or people and the review shows it only as "Held back as personal: <date> · <reason>"; the owner's `include <date>`, `include personal`, `hold <date>` and `forget <date>` map to `finalize.py --include / --include-personal / --hold / --forget-session` (a date shared by two sessions is refused with the uuids); a roll-up written with a held session, or without an included one, is marked stale, staged with a placeholder body and refused by `--commit` until that project's roll-up is re-run.
- report-feedback: `--auto` mode for agent-initiated bug reports — honors the owner's `state/feedback-prefs.json` toggles (auto-report + send-logs, both default on), dedupes identical titles (24h), and caps volume (5/day).
- turn-ledger: `src/turn_ledger.py` + the Stop hook enforce "a turn must end in a message or an explicit no-send" — an append-only ledger records every `room_ops.py say` and explicit no-send, `check-pending-tasks.sh` gates on it once a task-queue-empty turn would otherwise go silently idle. Session-scoped (PR #4028 review, qingyun-wu): every function defaults its `session` to `$CLAUDE_CODE_SESSION_ID` (same idiom as `scripts/skill-read-receipt.py`), so the reminder/boundary live in a per-session file and a ledger entry tagged for one session no longer resets or satisfies another's gate when two sessions share a workspace — reaches `room_ops.py say` and a manual `no-send` too, not just the two hooks, since neither needs to parse its own stdin payload. No session anywhere (outside Claude Code) reproduces the original shared-file behavior exactly. Pinned by a production-path test that runs the real hook scripts as subprocesses against one pinned workspace copy (`SUTANDO_TEST_MODE`), simulating two real sessions.

- task queue: `src/task_queue.py` owns the pending list (`pending` / `position` / `state/task-queue.json`); the watcher prints `QUEUE: <n> pending after this` under `TASK_FILE:` when other tasks wait, the activity card's queued row reads `queued · N ahead`, `activity.py queue --task-file` prints `{depth, position}`, `core-status.sh` writes `pending`, the voice `work` tool returns `queuedAhead` with the "Got it, N ahead of this one, working in order" line, and `get_core_status` reports how many tasks are queued. Only an absent `tasks/` reads as an empty queue: one that cannot be read (permissions) is never reported as zero — no `QUEUE:` line, a plain `queued` row, `core-status.json` without `pending`, and `state/task-queue.json` left as it was rather than overwritten with a count that was not taken.
- image-generation: the skill is enabled and documented for core; `generate.py` calls the Gemini image model over REST with the standard library (no `google-genai`, Pillow optional), takes any Gemini key the credential resolver finds (new `gemini-image` capability: managed text, then managed voice, then env), writes under `<workspace>/results/media/`, and prints one JSON line (`ok`/`no_key`/`refused`/`no_image`/`api_error`) with the verbatim owner message per failure in SKILL.md.
- conduct rules in CLAUDE.md: reply where you were asked (the DM exception is a closed list of connected-account and device data; web research stays in the room, with one "I sent it to you in our DM." line when an answer moves), reply in the owner's language, state blockers plainly, check the catalog and skills before saying a capability does not exist, and name the queue position once per task. `docs/built-in-tools.md` gains switch-model triggers, check quota, generate an image and activate a cloud tool.

## Fixed
- native Calendar/Reminders/Contacts need the owner's consent (user report: the desktop app raised a macOS Calendar permission prompt nobody asked for). The morning briefing no longer falls back to Calendar.app or launches it — the Google cache is the default, `MORNING_BRIEFING_CALENDAR_SOURCE=macos` is the owner's opt-in for the local apps, a denial (`-1743`) is recorded once and never re-asked, and without a source the briefing says "no calendar source is configured. Connect Google Calendar via Settings → Integrations, or set MORNING_BRIEFING_CALENDAR_SOURCE=macos to use the local Calendar app" and "Reminders not read: the local Reminders app is opt-in". **Migration:** a host whose briefing relied on the local Calendar/Reminders (no Google connector) sets `MORNING_BRIEFING_CALENDAR_SOURCE=macos` once (or the owner runs `python3 skills/macos-tools/scripts/native_pim_consent.py grant`); nothing else changes. `skills/macos-tools` scripts refuse without `--owner-asked` / the owner's `grant` marker (exit 2) and exit 3 on a stored or fresh denial with no retry; `grant` clears stored denials. The voice/phone `call_contact` tool no longer opens Contacts.app on its own: it needs the same host opt-in and answers with what the owner must do otherwise. New `hooks/native-pim-guard.py` (PreToolUse on Bash, auto-registered) denies `osascript`/JXA (app names and bundle ids, script files), `open -a`/`-b`/`.app`/`x-apple-reminderkit://` and `shortcuts run` against those apps unless the command carries `SUTANDO_ALLOW_NATIVE_PIM=1` at command position or the `grant` marker exists — and ignores that consent when the session's bound task is not owner-tier. The consent is self-attested where no task is bound (a guard against acting unasked, not access control; Claude-runtime only, a Codex core has the script gate alone). `friction-detector.py` treats reminders.py's no-consent exit as the probe being off, not a failed check. The consent record is the owner's alone: the hook also denies every other way of writing `state/native-pim-consent` or deleting a `*-automation-denied` marker (`touch`, redirects, `tee`, `rm`, `python3 -c "…grant()"`, importing `native_pim_consent`), while read-only commands that merely mention an app command or the marker (`grep`, `git log -S`, `cat`) now pass; the scripts refuse `--owner-asked`, the env var and the marker alike on a non-owner bound task; `call_contact` asks `native_pim_consent.py check`/`report-error` instead of carrying a TypeScript copy of the policy, and does not open Contacts when that check cannot run; the briefing runs without the macos-tools skill (calendar and reminders "not read", with the reason).
- connect-apps: the precheck line says `reply_to=room|dm`, and the skill's routing rule is the closed DM list (an apartment search asked in a shared room is answered there); marketplace: "use it now" precedes the report step so a newly activated cloud tool is never announced as needing a dashboard or a restart the script did not print.
- turn-ledger: a reply sent through the ag2-space MCP (`room.action.execute`) now counts as the turn speaking — the Stop gate reads `state/room-actions.jsonl`, written by the desktop MCP proxy after each successful room action, so it no longer refuses a turn that did reply.
- import-claude-context: the documented onboarding trigger now matches every request sentence the desktop actually sends — the consent time is optional (`(time not recorded)`), the standalone late-send form is documented, and matching is case-insensitive. Before this, a consent whose time was never recorded did not match the trigger at all. ([#4181])

<!-- fix() PRs go here -->
- import-claude-context: the import starts in the same turn as the hello. The onboarding trigger is now the owner's first-contact DM message ("… Then import my Claude Code history: I consented on the onboarding card at <ISO> …"), the Settings sentence is a user ask, and the desktop's task-file form (`channel_id: onboarding-wizard`, `priority: low`) stays as a legacy trigger; step 0 no longer says the import "can wait a minute" — on a fresh v0.6.8-rc3 install the core answered the greeting, finished the startup ceremony and left the consented task in `tasks/` until the owner nudged it (nothing re-delivers a task the watcher has emitted; `priority: low` orders, it does not hold). task-orphan-check: step 2 is now `scripts/classify.py` (read-only, tested), and a consented import task is never archived as an orphan once its run has started (`data/claude-import/status.json` newer than the task → `import-resume`, left for the watcher's sweep); an unstarted one gets a 30-minute line instead of 5. An import task is one carrying a run intent (the wizard header, `/import-claude-context`, or a documented trigger sentence), never a bare path mention; the terminal phases `staged` / `discarded` / `forgot` count as done like `done`; and a started run whose `status.json` has not moved for an hour is `import-stalled` — still resumed by the sweep, but listed in the recovery DM. `status.json` now names whose run it reports: `index.py --task-id <id>` mints a run (`task_id`, `run_id`) into `state.json` and every phase write carries both, and the orphan check counts a status for a task only when its `task_id` matches — an older run ending after a new import request was queued no longer archives that request as done (it is an orphan with the re-queue line), and a status with no task id keeps the task (`import-unbound`, listed in the recovery DM) rather than archiving it.
- report-feedback: read the desktop host's Keychain session (origin-scoped `AG2_CLOUD_TOKEN_*` key) so filing works on Tauri installs, and default the cloud origin to `sutando.ag2.space` (the retired `.ai` host drops the bearer across its redirect).
- web voice: playback gets its own AudioContext at the configured output rate (24 kHz by default) instead of sharing the device-rate capture context, so the browser no longer linearly interpolates each chunk and mirrors sibilants into a 16–20 kHz "beep" on 48 kHz devices; capture is unchanged; playback falls back to the device rate (with a debug warning) where the browser rejects the rate, and a renegotiated output rate recreates the playback context. ([#4240])

## Changed

<!-- refactor(), perf(), docs() changes visible to operators go here -->

## Security

<!-- security fixes go here -->
